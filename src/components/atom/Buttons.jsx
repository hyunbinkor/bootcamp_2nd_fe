const KakaoButton = (props) => {
  return <button className="btn btn-warning w-full">카카오로 로그인</button>;
};

const NaverButton = (props) => {
  return <button className="btn btn-success w-full">네이버로 로그인</button>;
};

export { KakaoButton, NaverButton };
